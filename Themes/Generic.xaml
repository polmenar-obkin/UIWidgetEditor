<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:UIWidgetEditorPlugin"
    xmlns:ctrls="clr-namespace:Frosty.Controls;assembly=FrostyControls"
    xmlns:core="clr-namespace:Frosty.Core.Controls;assembly=FrostyCore">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/FrostyCore;component/Themes/Generic.xaml" />
    </ResourceDictionary.MergedDictionaries>
    
    <!-- UI Widget Editor Style -->
    <Style TargetType="{x:Type local:UIWidgetEditor}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:UIWidgetEditor}">
                    <Grid x:Name="PART_MainGrid">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" MinWidth="200" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" MinWidth="200" />
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="20" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!-- Menu bar -->
                            <Border BorderThickness="1" BorderBrush="{DynamicResource ControlBackground}">
                                <DockPanel Grid.Row="0" Background="{DynamicResource ControlPressed}">
                                    <Button x:Name="PART_PropertyGridVisibilityButton" HorizontalAlignment="Right" DockPanel.Dock="Right" Width="20" ToolTip="Expand Property Grid">
                                        <Image Source="/UIWidgetEditorPlugin;component/Images/left-arrow-expand.png" />
                                    </Button>
                                </DockPanel>
                            </Border>

                            <Grid Grid.Row="1" x:Name="PART_WidgetViewGrid" ClipToBounds="True">
                                <!-- Widget view -->
                                <Viewbox x:Name="PART_WidgetViewBox" Margin="5">
                                    
                                </Viewbox>
                            </Grid>
                        </Grid>

                        <GridSplitter x:Name="PART_GridSplitter" Grid.Column="1" Width="2" VerticalAlignment="Stretch" HorizontalAlignment="Center" Background="Black" Visibility="Hidden" />

                        <!-- Property Grid -->
                        <Grid Grid.Column="2" x:Name="PART_PropertyGrid" Visibility="Hidden">
                            <ctrls:FrostyDockablePanel Title="Properties" Icon="/FrostyEditor;component/Images/Properties.png">
                                <core:FrostyPropertyGrid x:Name="PART_AssetPropertyGrid" Asset="{Binding Asset, RelativeSource={RelativeSource TemplatedParent}}" Modified="{Binding AssetModified, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" />
                            </ctrls:FrostyDockablePanel>
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>